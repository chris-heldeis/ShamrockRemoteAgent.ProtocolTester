<UserControl x:Class="ShamrockRemoteAgent.MasterTester.Views.ClientConnectRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="6">

        <!-- Card -->
        <Border Background="#020617"
                CornerRadius="12"
                Padding="24"
                Width="620"
                HorizontalAlignment="Left"
                BorderBrush="#1E293B"
                BorderThickness="1">

            <Border.Effect>
                <DropShadowEffect BlurRadius="18"
                                  ShadowDepth="2"
                                  Opacity="0.35"
                                  Color="Black"/>
            </Border.Effect>

            <StackPanel>

                <!-- Header -->
                <TextBlock Text="Client Connect Request"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#E5E7EB"
                           Margin="0,0,0,4"/>

                <TextBlock Text="Build and send a client connection packet"
                           Foreground="#94A3B8"
                           Margin="0,0,0,20"/>

                <!-- Device ID -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="Device ID"
                               FontWeight="Medium"
                               Foreground="#CBD5E1"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="DeviceIdBox"
                             Height="36"
                             FontSize="18"
                             Text="1"
                             Background="#020617"
                             Foreground="#E5E7EB"
                             BorderBrush="#334155"/>
                </StackPanel>

                <!-- Protocol -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="Protocol"
                               FontWeight="Medium"
                               Foreground="#CBD5E1"
                               Margin="0,0,0,6"/>
                    <ComboBox x:Name="ProtocolBox"
                              FontSize="18"
                              Height="36"
                              Padding="4"
                              HorizontalContentAlignment="Left">
                    </ComboBox>
                </StackPanel>

                <!-- TX Buffer -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="TX Buffer Size"
                               FontWeight="Medium"
                               Foreground="#CBD5E1"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="TxBufBox"
                             Height="36"
                             FontSize="18"
                             Text="0"
                             Background="#020617"
                             Foreground="#E5E7EB"
                             BorderBrush="#334155"/>
                </StackPanel>

                <!-- RX Buffer -->
                <StackPanel Margin="0,0,0,16">
                    <TextBlock Text="RX Buffer Size"
                               FontWeight="Medium"
                               Foreground="#CBD5E1"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="RxBufBox"
                             Height="36"
                             FontSize="18"
                             Text="0"
                             Background="#020617"
                             Foreground="#E5E7EB"
                             BorderBrush="#334155"/>
                </StackPanel>

                <!-- Self Packetize -->
                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="Self Packetize"
                               FontWeight="Medium"
                               Foreground="#CBD5E1"
                               Margin="0,0,0,6"/>
                    <ComboBox x:Name="SelfPacketizeBox"
                              FontSize="18"
                              Height="36">
                    </ComboBox>
                </StackPanel>

                <!-- Action -->
                <Button Content="Build Packet"
                        Height="40"
                        Width="160"
                        HorizontalAlignment="Center"
                        FontWeight="SemiBold"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
